This allows you to combine arrays without modifying either of the originals. 

```
let cats = ['blue', 'kitty']
let dogs = ['rusty', 'wyatt']

let pets = cat.concat(dogs)

pets
> ["blue", "kitty", "rusty", "wyatt"]
```